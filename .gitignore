# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build output
.next/
dist/
build/

# Logs
*.log
npm-debug.log*

# OS / temp
Thumbs.db
*.tmp

# Pipeline cache — committed (build-don't-buy evidence, plus
# saves teammates re-fetching). Aerial images are public satellite
# tiles from Esri/Google; geocode results contain only addresses.
# !intermediate/ is committed; teammates can also npm run fetch-aerial-free
# to repopulate from scratch.

# Pipeline outputs — committed: per-property submission artifacts.
# !outputs/ is committed.

# Scratch / preview output
tmp/
